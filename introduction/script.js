/*
 */
/*------------------------Change the document title to Modifying the DOM-----------------*/

document.title = "Modifying the DOM";

// todo: Change the background color of the body:

document.body.style.backgroundColor = "#DFF0FF";

// todo: Using the children method and a for ... of loop, display all children elements of the <body>:

// let bodychildren = document.body.children;
// for (let element of bodychildren) {
//   console.log(element);
// }
